# The workflows run on Linux runners, so anything they execute must be LF.
* text=auto eol=lf
*.png binary
*.pdf binary
*.xlsx binary
*.xls binary
*.gz binary

# Fonts are binary; without this they are line-ending normalised into rubble.
*.ttf binary
*.otf binary
*.woff2 binary
